#!/usr/bin/python
import cgi, random, glob


def main():
    form = cgi.FieldStorage()

    if 'questionNumber' in form:
       questionNumber = form["questionNumber"].value
    else:
	questionNumber = 0       		      	
